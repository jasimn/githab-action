name: 20 Use Composite Action

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use my custom action
        uses: ./composite-cache-deps
        with:
          node-version: 20.x
          working-dir: .
